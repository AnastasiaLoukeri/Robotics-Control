These files are associated with the package Assignment3.
Put the .py files inside the "scripts" directory and give them execution rights: chmod +x *.py
Put the myparams.yaml inside the "config" directory
Put the Info.msg inside the "msg" directory
Put the test.launch file inside the "launch" directory


